const initialState = {};

const reducer = (state = initialState, action, info) => {
  if (action.type === "GetUserData") {
    return state;
  } else {
    return state;
  }
};
export default reducer;
